# Python 3.12 recommended. Use a separate environment from main.ipynb.
# These three versions were recorded by the earlier completed experiment.
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.6.1

# Reproduction support pins selected for this package. The earlier output
# did not record these versions, so this is NOT a recovered historical lock.
scipy==1.15.3
matplotlib==3.10.8
seaborn==0.13.2
joblib==1.5.3
