# Optional local process-split files

`main.ipynb` can read the four previously exported split CSVs from this directory. The earlier `baseline.ipynb` instead expects the original concatenated CSV. See [the data instructions](../README.md) for the exact filenames. This README keeps the directory visible in Git without bundling the data.
